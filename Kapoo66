for _, v in pairs(game:GetService("Players").LocalPlayer.PlayerGui.SettingsUI.MainFrame.Frame.Content.SettingsTabFrame.Toggle_AutoQuestRepeat.SettingsHolder:GetDescendants()) do
    if v:IsA("ImageButton") then
        pcall(function()
           
            
 for _, c in pairs(getconnections(v.Activated)) do
                c.Function()
            end
        
            
           
        end)
    end
end

game:GetService("Players").LocalPlayer.PlayerGui.SettingsUI.MainFrame.Frame.Content.SettingsTabFrame.Toggle_AutoQuestRepeat.SettingsHolder.On
