while wait(10) do
for _, char in pairs(game.Workspace:GetChildren()) do
    if char:IsA("Model") and char.Name ~= game.Players.LocalPlayer.Name then
        char:Destroy()
    end
end 
for i,v in pairs(workspace:GetChildren()) do
if v.Name == "active"  then v:Destroy() end end end
